% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/functions.R
\name{os_cut}
\alias{os_cut}
\title{Цензурирование данных о выживаемости}
\usage{
os_cut(date_end, date_start, status, cens_time, cutoff_time, data)
}
\arguments{
\item{date_end}{Дата окончания наблюдения или события (в формате Date).}

\item{date_start}{Дата начала наблюдения (в формате Date).}

\item{status}{Статус события (0 - цензурировано, 1 - событие произошло).}

\item{cens_time}{Единица времени для цензурирования (число, например, 1 для дней, 30 для месяцев).}

\item{cutoff_time}{Время отсечки для цензурирования (число).}

\item{data}{Датафрейм, содержащий исходные данные.}
}
\value{
Датафрейм с добавленными колонками: \verb{os_status_<cutoff_time>}, \verb{os_time_<cutoff_time>} и \code{fu_time}.
}
\description{
Функция выполняет цензурирование данных о выживаемости на основе заданного времени отсечки.
}
\examples{
# Пример использования функции os_cut
data <- data.frame(
  date_start = as.Date("2020-01-01"),
  date_end = as.Date("2021-06-01"),
  status = c(1, 0, 1)
)
result <- os_cut(
  date_end = data$date_end,
  date_start = data$date_start,
  status = data$status,
  cens_time = 30,        # Время измеряется в месяцах
  cutoff_time = 12,      # 12 месяцев
  data = data
)
}
